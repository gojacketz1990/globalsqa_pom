class DemoSitePageLocators():
    tabs_locator = [
        ("link_text", "Tabs"),
    ]

    slider_locator = [
        ("link_text", "Slider"),
    ]

    tooltip_locator = [
        ("link_text", "ToolTip"),
    ]

    alertbox_locator = [
        ("link_text", "AlertBox"),
    ]

    dialogbox_locator = [
        ("link_text", "DialogBox"),
    ]
    progressbar_locator = [
        ("link_text", "ProgressBar"),
    ]
    frames_locator = [
        ("link_text", "Frames"),
    ]
    dropdown_locator = [
        ("link_text", "DropDown"),
    ]
    autocomplete_locator = [
        ("link_text", "AutoComplete"),
    ]
    selectelements_locator = [
        ("link_text", "SelectElements"),
    ]
    sorting_locator = [
        ("link_text", "Sorting"),
    ]
    spinner_locator = [
        ("link_text", "Spinner"),
    ]
    toolbar_locator = [
        ("link_text", "Toolbar"),
    ]
    datepicker_locator = [
        ("link_text", "DatePicker"),
    ]
    dragdrop_locator = [
        ("link_text", "DragAndDrop"),
    ]
    draggable_box_locator = [
        ("link_text", "DraggableBox"),
    ]
