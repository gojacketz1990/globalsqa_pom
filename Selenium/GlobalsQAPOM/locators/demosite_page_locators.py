class DemoSitePageLocators():
    tabs_locator = [
        ("link_text", "Tabs"),
    ]

    slider_locator = [
        ("link_text", "Slider"),
    ]

    tooltip_locator = [
        ("link_text", "ToolTip"),
    ]

    alertbox_locator = [
        ("link_text", "AlertBox"),
    ]

    dialogbox_locator = [
        ("link_text", "DialogBox"),
    ]
    progressbar_locator = [
        ("link_text", "ProgressBAR"),
    ]
